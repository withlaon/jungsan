# Vercel 정적 배포

이 폴더는 Vercel에 정적 `index.html`만 배포할 때 사용합니다.

## 배포 방법

### 1. Vercel CLI
```bash
cd vercel-static
vercel
```

### 2. Vercel 대시보드
- GitHub 저장소 연결 시 **Root Directory**를 `vercel-static`로 설정

### 3. 전체 Next.js 앱 배포
- Root Directory를 `jungsan-app`로 설정하면 Next.js 앱이 배포됩니다.
